<div class="character-sheet-container">
        <h2>Create a new character!</h2>
        <!-- Form for character customization -->
        <form id="character-form">
            <label for="name">Name:</label>
            <input type="text" id="name" name="name" required>

            <label for="level">Level:</label>
            <input type="number" id="level" name="level" required><br><br>
        <select id="avail-classes">
        {{#each availClass as | avClass |}}
            <option class="availClass" id="selectedclass" value={{avClass.id}}>
                <h2>{{avClass.class_name}}</h2>
            </option>
        {{/each}}
        </select>

        <select id="avail-races">
        {{#each races as | race |}}
            <option class="availRace" id="selectedrace" value={{race.id}}>
                <h2>{{{race.race_name}}}</h2>
            </option>
        {{/each}}
        </select>
            

            <label for="maxHP">Max HP:</label>
            <input type="number" id="maxHP" name="maxHP" required><br><br>

            <label for="currentHP">Current HP:</label>
            <input type="number" id="currentHP" name="currentHP" required><br><br>
 
            <label for="description">Description:</label>
            <textarea id="description" name="description" required></textarea><br><br>

            <label for="age">Age:</label>
            <input type="number" id="age" name="age" required><br><br>

            <label for="features">Features:</label>
            <textarea id="features" name="features" required></textarea><br><br>
        </form>
    
        <!-- Save button -->
        <button id="save-button">Save Character</button>

        <!-- Dropdown for pre-built characters -->
        <label for="built-characters">Select a built character:</label>
        <select id="built-characters">
        {{#each characters as | character |}}
            <option class="character" id="selectedChar" value={{character.id}}>
                <h2>{{character.char_name}}</h2>
            </option>
        {{/each}}
        </select>
        <button id="char-button">Open Sheet</button>
    </div>    
    <script src="/assets/js/home.js"></script>
    <script src="/assets/js/script.js"></script>
